# Exemplo de configuração de variáveis de ambiente para produção
# Configure estas variáveis no painel do Render

# Segurança JWT - OBRIGATÓRIO gerar uma nova chave para produção
JWT_SECRET_KEY=sua_chave_secreta_super_forte_aqui_32_bytes_minimo

# Senha do administrador - OBRIGATÓRIO alterar
ADMIN_PASSWORD=sua_senha_admin_super_forte_aqui

# Porta do servidor (normalmente definida pelo Render automaticamente)
PORT=8000

# Ambiente (definido automaticamente pelo Render)
RENDER=true
